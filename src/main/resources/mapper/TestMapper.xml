<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="resource.NewsMapper2">
	<!-- 뉴스 타이틀  출력 -->
	<insert id="insertNews" parameterType="vo.TestVO">
		insert into bigkinds (newsname, title, category, date, url, content) 
		values(#{newsname},#{title},#{category},#{date},#{url},#{content})
	</insert>

</mapper>